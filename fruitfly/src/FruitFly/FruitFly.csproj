<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
  </PropertyGroup>

  <!-- The console app is just one viewer of the brain. It references the Core library
       rather than owning the neuron code, so the math has a single home. -->
  <ItemGroup>
    <ProjectReference Include="..\FruitFly.Core\FruitFly.Core.csproj" />
  </ItemGroup>
</Project>
